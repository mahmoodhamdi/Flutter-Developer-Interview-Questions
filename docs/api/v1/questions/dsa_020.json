{
  "id": "dsa_020",
  "question": "Write functions to perform in-order, pre-order, and post-order tree traversals.",
  "answer": "```dart\nclass TreeTraversals {\n  // In-order: Left -> Root -> Right (sorted order for BST)\n  static List<T> inOrder<T>(TreeNode<T>? node) {\n    if (node == null) return [];\n    return [\n      ...inOrder(node.left),\n      node.value,\n      ...inOrder(node.right),\n    ];\n  }\n\n  // Pre-order: Root -> Left -> Right (useful for copying tree)\n  static List<T> preOrder<T>(TreeNode<T>? node) {\n    if (node == null) return [];\n    return [\n      node.value,\n      ...preOrder(node.left),\n      ...preOrder(node.right),\n    ];\n  }\n\n  // Post-order: Left -> Right -> Root (useful for deletion)\n  static List<T> postOrder<T>(TreeNode<T>? node) {\n    if (node == null) return [];\n    return [\n      ...postOrder(node.left),\n      ...postOrder(node.right),\n      node.value,\n    ];\n  }\n}\n\n// For tree:      5\n//              /   \\\n//             3     7\n//            / \\\n//           1   4\n\n// In-order:   [1, 3, 4, 5, 7]\n// Pre-order:  [5, 3, 1, 4, 7]\n// Post-order: [1, 4, 3, 7, 5]\n```\n\n**Time Complexity**: O(n) for all traversals\n**Space Complexity**: O(h) where h is height (O(log n) balanced, O(n) worst)\n\n---",
  "difficulty": "intermediate",
  "tags": [
    "dsa"
  ],
  "code_example": "class TreeTraversals {\n  // In-order: Left -> Root -> Right (sorted order for BST)\n  static List<T> inOrder<T>(TreeNode<T>? node) {\n    if (node == null) return [];\n    return [\n      ...inOrder(node.left),\n      node.value,\n      ...inOrder(node.right),\n    ];\n  }\n\n  // Pre-order: Root -> Left -> Right (useful for copying tree)\n  static List<T> preOrder<T>(TreeNode<T>? node) {\n    if (node == null) return [];\n    return [\n      node.value,\n      ...preOrder(node.left),\n      ...preOrder(node.right),\n    ];\n  }\n\n  // Post-order: Left -> Right -> Root (useful for deletion)\n  static List<T> postOrder<T>(TreeNode<T>? node) {\n    if (node == null) return [];\n    return [\n      ...postOrder(node.left),\n      ...postOrder(node.right),\n      node.value,\n    ];\n  }\n}\n\n// For tree:      5\n//              /   \\\n//             3     7\n//            / \\\n//           1   4\n\n// In-order:   [1, 3, 4, 5, 7]\n// Pre-order:  [5, 3, 1, 4, 7]\n// Post-order: [1, 4, 3, 7, 5]",
  "topic_slug": "dsa",
  "category": "misc"
}
