{
  "id": "dsa_028",
  "question": "Implement Depth-First Search (DFS) for a graph using recursion.",
  "answer": "```dart\nclass DFS {\n  static List<int> dfs(Map<int, List<int>> graph, int start) {\n    List<int> result = [];\n    Set<int> visited = {};\n\n    void dfsRecursive(int node) {\n      if (visited.contains(node)) return;\n\n      visited.add(node);\n      result.add(node);\n\n      for (int neighbor in graph[node] ?? []) {\n        dfsRecursive(neighbor);\n      }\n    }\n\n    dfsRecursive(start);\n    return result;\n  }\n\n  // Iterative using stack\n  static List<int> dfsIterative(Map<int, List<int>> graph, int start) {\n    List<int> result = [];\n    Set<int> visited = {};\n    List<int> stack = [start];\n\n    while (stack.isNotEmpty) {\n      int node = stack.removeLast();\n\n      if (visited.contains(node)) continue;\n      visited.add(node);\n      result.add(node);\n\n      // Add neighbors in reverse order for consistent traversal\n      for (int neighbor in (graph[node] ?? []).reversed) {\n        if (!visited.contains(neighbor)) {\n          stack.add(neighbor);\n        }\n      }\n    }\n\n    return result;\n  }\n}\n\n// Usage\nvar graph = {\n  0: [1, 2],\n  1: [0, 3],\n  2: [0, 3],\n  3: [1, 2],\n};\nprint(DFS.dfs(graph, 0)); // [0, 1, 3, 2]\n```\n\n**Time Complexity**: O(V + E)\n**Space Complexity**: O(V)\n\n---",
  "difficulty": "intermediate",
  "tags": [
    "dsa"
  ],
  "code_example": "class DFS {\n  static List<int> dfs(Map<int, List<int>> graph, int start) {\n    List<int> result = [];\n    Set<int> visited = {};\n\n    void dfsRecursive(int node) {\n      if (visited.contains(node)) return;\n\n      visited.add(node);\n      result.add(node);\n\n      for (int neighbor in graph[node] ?? []) {\n        dfsRecursive(neighbor);\n      }\n    }\n\n    dfsRecursive(start);\n    return result;\n  }\n\n  // Iterative using stack\n  static List<int> dfsIterative(Map<int, List<int>> graph, int start) {\n    List<int> result = [];\n    Set<int> visited = {};\n    List<int> stack = [start];\n\n    while (stack.isNotEmpty) {\n      int node = stack.removeLast();\n\n      if (visited.contains(node)) continue;\n      visited.add(node);\n      result.add(node);\n\n      // Add neighbors in reverse order for consistent traversal\n      for (int neighbor in (graph[node] ?? []).reversed) {\n        if (!visited.contains(neighbor)) {\n          stack.add(neighbor);\n        }\n      }\n    }\n\n    return result;\n  }\n}\n\n// Usage\nvar graph = {\n  0: [1, 2],\n  1: [0, 3],\n  2: [0, 3],\n  3: [1, 2],\n};\nprint(DFS.dfs(graph, 0)); // [0, 1, 3, 2]",
  "topic_slug": "dsa",
  "category": "misc"
}
