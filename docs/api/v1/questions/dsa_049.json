{
  "id": "dsa_049",
  "question": "Implement the Fibonacci sequence using dynamic programming (both top-down and bottom-up).",
  "answer": "```dart\n// Top-Down with Memoization\nclass FibMemo {\n  final Map<int, int> _cache = {};\n\n  int fib(int n) {\n    if (n <= 1) return n;\n\n    if (!_cache.containsKey(n)) {\n      _cache[n] = fib(n - 1) + fib(n - 2);\n    }\n\n    return _cache[n]!;\n  }\n}\n\n// Bottom-Up with Tabulation\nint fibBottomUp(int n) {\n  if (n <= 1) return n;\n\n  List<int> dp = List.filled(n + 1, 0);\n  dp[1] = 1;\n\n  for (int i = 2; i <= n; i++) {\n    dp[i] = dp[i - 1] + dp[i - 2];\n  }\n\n  return dp[n];\n}\n\n// Space-Optimized Bottom-Up\nint fibOptimized(int n) {\n  if (n <= 1) return n;\n\n  int prev2 = 0, prev1 = 1;\n\n  for (int i = 2; i <= n; i++) {\n    int current = prev1 + prev2;\n    prev2 = prev1;\n    prev1 = current;\n  }\n\n  return prev1;\n}\n\n// Usage\nprint(FibMemo().fib(50));  // 12586269025\nprint(fibBottomUp(50));     // 12586269025\nprint(fibOptimized(50));    // 12586269025\n```\n\n**Time Complexity**: O(n) for all\n**Space Complexity**: O(n) for memo/tabulation, O(1) for optimized\n\n---",
  "difficulty": "intermediate",
  "tags": [
    "dsa"
  ],
  "code_example": "// Top-Down with Memoization\nclass FibMemo {\n  final Map<int, int> _cache = {};\n\n  int fib(int n) {\n    if (n <= 1) return n;\n\n    if (!_cache.containsKey(n)) {\n      _cache[n] = fib(n - 1) + fib(n - 2);\n    }\n\n    return _cache[n]!;\n  }\n}\n\n// Bottom-Up with Tabulation\nint fibBottomUp(int n) {\n  if (n <= 1) return n;\n\n  List<int> dp = List.filled(n + 1, 0);\n  dp[1] = 1;\n\n  for (int i = 2; i <= n; i++) {\n    dp[i] = dp[i - 1] + dp[i - 2];\n  }\n\n  return dp[n];\n}\n\n// Space-Optimized Bottom-Up\nint fibOptimized(int n) {\n  if (n <= 1) return n;\n\n  int prev2 = 0, prev1 = 1;\n\n  for (int i = 2; i <= n; i++) {\n    int current = prev1 + prev2;\n    prev2 = prev1;\n    prev1 = current;\n  }\n\n  return prev1;\n}\n\n// Usage\nprint(FibMemo().fib(50));  // 12586269025\nprint(fibBottomUp(50));     // 12586269025\nprint(fibOptimized(50));    // 12586269025",
  "topic_slug": "dsa",
  "category": "misc"
}
